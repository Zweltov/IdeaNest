# Фрагмент промпта для ИИ, пишущих статьи IdeaNest

Скопируй блок «ПРОМПТ» в системный или пользовательский промпт автору.

---

## ПРОМПТ

Пиши статью для сайта IdeaNest в **Markdown**. Текст попадёт в поле `text` таблицы `articles` (Supabase).

### Структура статьи
1. Краткое введение (2–4 предложения).
2. Основной разбор с подзаголовками `##` и `###`.
3. Если тема про деньги / запуск / модель — **один** блок бизнес-калькулятора `:::calc` (см. ниже).
4. Плюсы/минусы или чек-лист.
5. Короткий вывод без гарантий дохода.

### Семантические блоки

```
:::tip
Полезный совет
:::
```
Типы: `tip` (совет), `warning` (осторожно), `note` (заметка), `success` (удачный кейс), `danger` (критичный риск).

```
:::pros
Пункт плюс 1
Пункт плюс 2
:::

:::cons
Пункт минус 1
:::

:::checklist
Шаг 1
Шаг 2
:::
```

### Медиа
- Картинка: `![краткое описание](https://публичный-url.jpg)`
- YouTube: отдельной строкой `https://youtu.be/VIDEO_ID`  
  или `:::youtube https://youtu.be/VIDEO_ID :::`

### Бизнес-калькулятор (одна плашка, 4 вкладки)

Вставляй так:

```
:::calc
mode: payback
invest: 300000
monthly_profit: 40000
price: 1500
cost: 600
fixed: 80000
cac: 500
ltv: 3000
margin: 60
eq: 100000
rent: 50000
ads: 30000
payroll: 80000
other: 40000
:::
```

#### Строка `mode` — какая вкладка открыта сначала
| Значение | Вкладка |
|----------|---------|
| `payback` | Окупаемость |
| `breakeven` | Безубыточность |
| `unit` | Unit-экономика |
| `budget` | Бюджет запуска |

#### Поля и зачем они

**Вкладка «Окупаемость»**
| Поле | Смысл |
|------|--------|
| `invest` | Стартовые вложения, ₽ (можно писать `budget`) |
| `monthly_profit` | Ожидаемая **чистая** прибыль в месяц, ₽ (синонимы: `profit`, `monthly`) |

**Вкладка «Безубыточность»**
| Поле | Смысл |
|------|--------|
| `fixed` | Постоянные расходы в месяц, ₽ (аренда, оклады…) |
| `price` | Цена продажи одной единицы, ₽ |
| `cost` | Себестоимость одной единицы, ₽ (синоним: `cogs`) |

**Вкладка «Unit-экономика»**
| Поле | Смысл |
|------|--------|
| `cac` | Стоимость привлечения одного клиента, ₽ |
| `ltv` | Сколько в среднем принесёт клиент за всё время, ₽ |
| `margin` | Маржа в **процентах** (0–100), не в рублях |

**Вкладка «Бюджет»**
| Поле | Смысл |
|------|--------|
| `eq` | Оборудование / первичный закуп, ₽ |
| `rent` | Аренда на первый период, ₽ |
| `ads` | Реклама на старт, ₽ |
| `payroll` | Фонд зарплат на старт, ₽ |
| `other` | Прочее и финансовая подушка, ₽ |

Можно указать **не все** поля — только те, что относятся к теме статьи. Пустые читатель заполнит сам в интерфейсе.

#### Зачем тогда «реалистичные стартовые цифры», если читатель всё равно может менять?

Калькулятор — **интерактивный**: любой посетитель крутит поля и видит свой сценарий.

Но блок в статье задаёт **стартовую точку**, которую видят сразу, без ввода:

1. **Контекст темы** — у кофейни и у SaaS разные порядки сумм; нули или «1 ₽» выглядят глупо и не учат.
2. **Пример из статьи** — цифры должны совпадать с тем, что ты пишешь в тексте («вложили ~300 тыс.» → в `invest` тоже ~300000).
3. **Доверие** — осмысленный пример = статья полезнее; фантастический миллиард или пустые поля = шум.

Итого: автор задаёт **разумный пример по теме**; читатель подставляет **свои** цифры. Это не «заморозка ответа», а заготовка сценария.

Правила для цифр:
- рубли, порядок величин как в РФ для этой ниши;
- не обещай «гарантированную прибыль» в тексте рядом с калькулятором;
- `mode` выбирай по главному вопросу статьи (окупаемость / сколько продавать / сходится ли unit / смета запуска).

### Метаданные (не в Markdown, а отдельные колонки в `articles`)
- `title` — заголовок  
- `slug` — латиница-кебаб: `kak-otkryt-kofeynyu`  
- `description` — 1–2 предложения для превью  
- `main_idea_id` — id идеи из таблицы `ideas`, если статья про неё  

### Стиль
Конкретно, без воды и кликбейта. Риски называть прямо. Не давать юридических/финансовых гарантий.

---

## В планах (ещё не сделано на сайте)

Имеет смысл заложить в дорожную карту, не требовать от ИИ в текущих статьях:

- загрузка фото в статьи с компьютера (сейчас только URL картинки);
- страница «Конфиденциальность» и нормальные «Об авторе» / «Контакты» в футере;
- синхронизация навбара и аккаунтов между устройствами (сейчас localStorage браузера);
- форма «написать статью» на самом сайте, без Table Editor;
- обложки идей (`cover_url`) массово + подбор картинок;
- больше интерактива в Markdown (аккордеон, спойлер, сортируемая таблица, таймлайн, мастер выбора);
- отдельные сохранённые сценарии калькулятора («мой расчёт») у авторизованных;
- мобильное меню навбара с теми же пунктами, что на десктопе;
- i18n (реальный перевод интерфейса, не только запись языка в БД);
- настоящее удаление аккаунта через Edge Function (сейчас soft-delete).
